# Master Document Example

## Purpose

This folder contains Master Document preview payloads used by the Idea -> Master Document sandbox.

## Contents

Canonical expected payload:

- [`master_document_preview_payload.md`](./master_document_preview_payload.md)

## How to Use

Use this payload to understand the preview output shape shown by the sandbox. It is a source-backed demo payload, not a production product document.

## Canonical vs Supporting

The payload supports the example sandbox. Canonical framework concepts live in [`../../docs/`](../../docs/), and reusable structures live in [`../../templates/`](../../templates/).

## Not Included / Do Not Confuse

This folder does not contain prompt bodies, website code, or a complete application specification.
